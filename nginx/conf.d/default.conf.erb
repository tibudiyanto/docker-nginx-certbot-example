server {
    listen 80;
    server_name <%= ENV["DOMAIN_NAME"] %>;
    return 301 https://$server_name$request_uri;
}

server {
    listen       443 ssl;
    server_name  <%= ENV["DOMAIN_NAME"] %>;

    location / {
        # Your config here!
    }
}
